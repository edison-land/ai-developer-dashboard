{
  "name": "@ai-dashboard/desktop",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build:main": "node scripts/build-main.mjs",
    "package:win": "pnpm --filter @ai-dashboard/ui build && pnpm build:main && electron-builder --win nsis --x64 --publish never",
    "package:mac": "pnpm --filter @ai-dashboard/ui build && pnpm build:main && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --universal --publish never",
    "start": "pnpm --filter @ai-dashboard/ui build && pnpm build:main && electron .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-dashboard/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "build": {
    "appId": "com.aideveloperdashboard.desktop",
    "productName": "AI Developer Dashboard",
    "artifactName": "AI-Developer-Dashboard-v1.1.1-setup-${arch}.${ext}",
    "asar": true,
    "directories": {
      "output": "../../dist/v1.1.1"
    },
    "files": [
      "dist/main.cjs",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../packages/ui/dist",
        "to": "ui"
      }
    ],
    "win": {
      "requestedExecutionLevel": "asInvoker",
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "mac": {
      "sign": null,
      "notarize": false,
      "artifactName": "AI-Developer-Dashboard-v${version}-mac-${arch}.${ext}",
      "category": "public.app-category.developer-tools",
      "target": [
        {
          "target": "dmg",
          "arch": ["universal"]
        },
        {
          "target": "zip",
          "arch": ["universal"]
        }
      ]
    },
    "dmg": {
      "contents": [
        {
          "x": 130,
          "y": 220,
          "type": "file"
        },
        {
          "x": 410,
          "y": 220,
          "type": "link",
          "path": "/Applications"
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "AI Developer Dashboard",
      "deleteAppDataOnUninstall": false,
      "runAfterFinish": false
    }
  }
}
