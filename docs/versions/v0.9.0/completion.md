# v0.9.0 完成情况：UI 回归保护

> 状态：🟡 计划中
> 实施尚未开始。

## 当前基线

- core 和 server 已有自动测试。
- 当前全仓共有 122 项测试。
- UI 只有类型检查、生产构建和人工检查，没有组件级或端到端自动回归。
- `pnpm verify:ui-redesign` 当前执行类型检查、测试和 UI 构建。

## 启动条件

只有 v0.8.0 完成后才开始本版本，避免为仍在变化的错误行为固化 UI 测试。

## 完成后必须填写

- 采用的 UI 测试工具；
- 覆盖的用户流程；
- 新增测试数与统一验收命令；
- 异常场景结果；
- 故障排查和发布检查表链接；
- 未覆盖的风险。

在关键流程自动回归和统一验收接通前，本版本不得标记为已完成。
