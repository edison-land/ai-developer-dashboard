# v2.0.0 完成情况：项目任务卡驱动对话执行

> 状态：💡 远期规划
> 记录日期：2026-07-29
> 尚未批准实施，也没有排期。

## 当前情况

- 已把 2026-07-29 的产品讨论整理为正式需求。
- 已确认“拖动任务卡后自动开启或继续对话”属于 v2.0.0。
- 已区分项目任务卡与现有项目阶段，避免自动执行静默覆盖人工阶段。
- 已记录 CLI/headless、已有对话注入、worktree 和状态回写等关键调研方向。
- 当前代码没有项目任务卡、自动对话调度或执行状态回写能力。

## 尚未完成

- 没有任务卡数据模型和页面；
- 没有创建或继续对话的稳定接口；
- 没有 Prompt 预览与上下文组装；
- 没有任务、对话和 worktree 的映射；
- 没有执行状态监听、失败重试或防重复机制；
- 没有真实 Agent 工具的端到端验证。

## 启动条件

1. 用户明确批准进入 v2.0.0；
2. 确认首个支持的 Agent 工具；
3. 完成新建对话、继续对话和状态读取的可行性验证；
4. 确认 worktree 与权限策略；
5. 明确 v1.0.0 桌面壳是否是前置条件；
6. 把远期需求收敛为可以分阶段交付的实施计划。

## 建议的验证顺序

1. 先实现按项目保存未发送消息的一键复制原型；
2. 验证用户是否持续使用任务卡记录灵感；
3. 再验证 CLI/headless 自动开启新对话；
4. 然后验证向已有对话注入 Prompt；
5. 最后接入状态回写、失败恢复和 worktree 追踪。

## 验证状态

- 文档结构与链接：2026-07-29 已通过 `docs-by-version` 校验，0 个错误、0 个警告；
- Markdown 与 Git 差异格式：`git diff --check` 通过；
- 产品需求：已记录，待用户后续评审；
- 技术可行性：未验证；
- 实现与端到端验收：未开始。
