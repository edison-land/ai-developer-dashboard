# v0.4.0 完成情况：阶段管理

> 状态：✅ 已完成
> 完成时间：2026-07-28 前的基础能力，随后持续保留

## 实际完成

- 领域模型建立五阶段固定集合。
- SQLite `stage_overrides` 保存人工覆盖。
- 聚合层实现“人工覆盖优先于 AI 缓存”。
- 服务端提供阶段 PATCH/DELETE 接口。
- UI 提供阶段视图和人工阶段选择。
- 项目详情中可以切回“自动判断”。

## 完成依据

这个文档里程碑用于把原综合 PRD 的 Phase 4 独立出来，不对应单独发布 tag：

- 阶段接口和存储基础来自初始提交 `794da17`；
- AI 阶段来源由 `c39a2e0` 补齐；
- 当前实现位于 `StageBoard`、`ProjectDetailsModal`、聚合层和 store。

## 验证记录

- store 测试覆盖阶段写入、读取和清除。
- aggregate 测试覆盖人工阶段优先和 AI 阶段回退。
- server 测试覆盖合法写入与非法阶段拒绝。

## 后续变化

- v0.5.0 曾加入阶段列悬停展开。
- v0.6.0 主动移除悬停展开，保留紧凑阶段视图，并把完整信息放入项目详情。
- 阶段管理能力本身没有被移除。
