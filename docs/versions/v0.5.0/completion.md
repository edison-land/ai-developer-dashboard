# v0.5.0 完成情况：日常工作流补齐

> 状态：✅ 已完成
> 完成日期：2026-07-28
> 主要提交：`a128538`、`0fc02c3`

## 实际完成

- 新增纯函数活动流和 `GET /api/activity`。
- 新增自动机械刷新，刷新后同步使活动数据失效重取。
- 新增来源多选和时间预设筛选，保存到 localStorage。
- 新增 `archives` 表、归档/恢复接口和归档抽屉。
- 归档项目默认从项目与活动中排除。
- 按原需求实现阶段列 hover 展开。
- 完成对比度、字号、层次、数字、减少动效和表单语义优化。

## 验证记录

阶段交付时：

- 98 项测试通过（core 80 + server 18）；
- 真实数据生成 24 条合并动态；
- 归档 → 默认排除 → 包含归档 → 恢复的往返验证净结果为零；
- UI 类型检查和生产构建通过。

当时保留的 4 项既有类型问题随后在 v0.6.0 重设计中修复；当前全仓类型检查通过。

## 与原需求的后续关系

R1 来源筛选、R2 时间筛选和 R3 归档继续保留。

R4 悬停展开在本版本确实完成，但 v0.6.0 的聚焦式重设计认为它增加信息噪声和交互复杂度，因此主动替换为：

- 紧凑阶段视图；
- 点击项目打开完整详情。

这属于后续设计替代，不是 v0.5.0 未完成。

## 遗留到后续版本

- 首页信息密度仍高，进入 v0.6.0 重设计。
- 自动刷新只采集机械数据；AI 联动进入 v0.8.0。
