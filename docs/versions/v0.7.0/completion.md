# v0.7.0 完成情况：批量总结实时进度

> 状态：✅ 已完成
> 完成日期：2026-07-28
> 主要提交：`58da9df`

## 实际完成

- `/api/synthesize-all` 改为 NDJSON 流式响应。
- 每个项目完成后立即回传项目和累计统计。
- 服务端保持串行处理并允许单项失败后继续。
- UI 流式解析事件，立即更新 React Query 中对应项目。
- 顶部按钮与状态文字显示完成总数、更新、缓存和失败。
- 新增 `scripts/qa-live-synthesis-progress.ts`，用三个内存项目演示，不读取真实 Key 或用户数据。

## 验证记录

服务端自动测试覆盖：

- 完整统计；
- 第一项先于未完成的第二项到达；
- stale 项目包含、归档项目排除；
- 单项失败后继续；
- 最大并发为 1。

2026-07-29 的全仓复验中，相关测试包含在 122 项通过结果内。

## 已知限制

- 页面刷新或连接断开后，当前批次不会恢复。
- 成本为零的 QA 环境已提供，但最新页面仍需人工浏览器走查记录。
- 真实模型冒烟继续归入 v0.8.0。
