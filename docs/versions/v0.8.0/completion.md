# v0.8.0 完成情况：现有能力名实一致

> 状态：🟡 计划中
> 实施尚未开始。

## 当前基线

已经具备：

- `synthOnRefresh` 设置字段和本地持久化；
- 手动/定时机械刷新；
- 单项目与逐项批量总结；
- 智谱 provider；
- 真实模型 smoke 脚本；
- 成本为零的批量进度 QA 环境。

尚未具备：

- 刷新流程与 AI 总结的实际接线；
- 设置页对未实现 provider 的严格限制；
- 可作为发布门槛的 smoke 输出和失败条件；
- 最新版本的真实模型与人工浏览器验收记录。

## 完成后必须填写

- 实际提交；
- 新增或修改的测试；
- 真实模型、日期和结果；
- 自动总结失败时的页面表现；
- 与 `requirements.md` 的任何偏离；
- 尚未完成且需要转入 v0.9.0 的事项。

在上述内容填写并通过验收前，本版本不得标记为已完成。
