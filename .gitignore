node_modules/
dist/
*.tsbuildinfo
.vite/
*.log
.DS_Store
# local dev data (never commit user's dashboard db / keys)
.ai-dashboard/
.env
.env.local
# 工具产物（playwright skill 缓存 / 截图）
.playwright-cli/
kanban.png

# Local project documentation (do not publish)
docs/
